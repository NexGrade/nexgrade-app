openapi: 3.1.0
info:
  title: Api
  version: 0.2.0
  description: GradeEscolar â€” Sistema Completo de GestÃ£o de HorÃ¡rios Escolares
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: professores
    description: GestÃ£o de professores
  - name: disciplinas
    description: GestÃ£o de disciplinas
  - name: turmas
    description: GestÃ£o de turmas
  - name: horarios
    description: GestÃ£o e geraÃ§Ã£o de horÃ¡rios
  - name: salas
    description: GestÃ£o de salas e espaÃ§os
  - name: licencas
    description: LicenÃ§as de professores e substituiÃ§Ãµes
  - name: comunicados
    description: Comunicados para turmas e professores
  - name: usuarios
    description: UsuÃ¡rios e perfis de acesso
  - name: audit
    description: HistÃ³rico de alteraÃ§Ãµes
  - name: configuracoes
    description: ConfiguraÃ§Ãµes do sistema
  - name: export
    description: ExportaÃ§Ã£o de dados
  - name: conflitos
    description: DetecÃ§Ã£o e sugestÃµes de conflitos
  - name: stats
    description: EstatÃ­sticas e resumos
  - name: calendario-escolar
    description: Calendario escolar, feriados e trimestres letivos

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # â”€â”€ PROFESSORES â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /professores:
    get:
      operationId: listProfessores
      tags: [professores]
      summary: Lista todos os professores
      responses:
        "200":
          description: Lista de professores
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Professor"
    post:
      operationId: createProfessor
      tags: [professores]
      summary: Cria um professor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProfessorInput"
      responses:
        "201":
          description: Professor criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Professor"

  /professores/{id}:
    get:
      operationId: getProfessor
      tags: [professores]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Professor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Professor"
        "404":
          description: NÃ£o encontrado
    patch:
      operationId: updateProfessor
      tags: [professores]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProfessorUpdate"
      responses:
        "200":
          description: Atualizado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Professor"
    delete:
      operationId: deleteProfessor
      tags: [professores]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removido

  /professores/{id}/carga:
    get:
      operationId: getProfessorCarga
      tags: [professores]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Carga horÃ¡ria
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfessorCarga"

  # â”€â”€ DISCIPLINAS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /disciplinas:
    get:
      operationId: listDisciplinas
      tags: [disciplinas]
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Disciplina"
    post:
      operationId: createDisciplina
      tags: [disciplinas]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisciplinaInput"
      responses:
        "201":
          description: Criada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Disciplina"

  /disciplinas-catalogo:
    get:
      operationId: listDisciplinasCatalogo
      tags: [disciplinas-catalogo]
      responses:
        "200":
          description: Lista do catalogo mestre
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DisciplinaCatalogo"
  /disciplinas-catalogo/adicionar-selecionadas:
    post:
      operationId: adicionarDisciplinasCatalogoSelecionadas
      tags: [disciplinas-catalogo]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdicionarDisciplinasCatalogoInput"
      responses:
        "201":
          description: Disciplinas adicionadas
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdicionarDisciplinasCatalogoResponse"
  /disciplinas/{id}:
    get:
      operationId: getDisciplina
      tags: [disciplinas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Disciplina
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Disciplina"
        "404":
          description: NÃ£o encontrada
    patch:
      operationId: updateDisciplina
      tags: [disciplinas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisciplinaUpdate"
      responses:
        "200":
          description: Atualizada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Disciplina"
    delete:
      operationId: deleteDisciplina
      tags: [disciplinas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removida

  # â”€â”€ TURMAS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /turmas:
    get:
      operationId: listTurmas
      tags: [turmas]
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Turma"
    post:
      operationId: createTurma
      tags: [turmas]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TurmaInput"
      responses:
        "201":
          description: Criada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Turma"

  /turmas/{id}:
    get:
      operationId: getTurma
      tags: [turmas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Turma
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Turma"
        "404":
          description: NÃ£o encontrada
    patch:
      operationId: updateTurma
      tags: [turmas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TurmaUpdate"
      responses:
        "200":
          description: Atualizada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Turma"
    delete:
      operationId: deleteTurma
      tags: [turmas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removida

  /turmas/{id}/aplicar-matriz:
    post:
      operationId: aplicarMatrizTurma
      tags: [turmas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AplicarMatrizInput"
      responses:
        "200":
          description: Turma com disciplinas e cargas horÃ¡rias substituÃ­das pelas da matriz (RF-TUR-02)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Turma"
        "404":
          description: Turma ou matriz curricular nÃ£o encontrada

  /turmas/{id}/horario:
    get:
      operationId: getTurmaHorario
      tags: [turmas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Grade horÃ¡ria
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HorarioSlot"

  # â”€â”€ HORÁRIOS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /horarios:
    get:
      operationId: listHorarios
      tags: [horarios]
      parameters:
        - name: turmaId
          in: query
          schema:
            type: integer
        - name: professorId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HorarioSlot"
    post:
      operationId: createHorario
      tags: [horarios]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HorarioSlotInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HorarioSlot"

  /horarios/{id}:
    delete:
      operationId: deleteHorario
      tags: [horarios]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removido

  /horarios/gerar:
    post:
      operationId: gerarHorario
      tags: [horarios]
      summary: Gera horÃ¡rio automaticamente com algoritmo avanÃ§ado
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GerarHorarioInput"
      responses:
        "200":
          description: Resultado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GerarHorarioResult"

  /horarios/experimentais:
    get:
      operationId: listHorariosExperimentais
      tags: [horarios]
      summary: Lista horÃ¡rios experimentais
      parameters:
        - name: turmaId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HorarioExperimental"
    post:
      operationId: createHorarioExperimental
      tags: [horarios]
      summary: Cria horÃ¡rio experimental
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HorarioExperimentalInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HorarioExperimental"

  /horarios/experimentais/{nome}/promover:
    post:
      operationId: promoverHorarioExperimental
      tags: [horarios]
      summary: Promove horÃ¡rio experimental para oficial
      parameters:
        - name: nome
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Promovido
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GerarHorarioResult"

  /horarios/experimentais/{nome}:
    delete:
      operationId: deleteHorarioExperimental
      tags: [horarios]
      summary: Remove horÃ¡rio experimental
      parameters:
        - name: nome
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Removido

  # â”€â”€ SALAS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /salas:
    get:
      operationId: listSalas
      tags: [salas]
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Sala"
    post:
      operationId: createSala
      tags: [salas]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalaInput"
      responses:
        "201":
          description: Criada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Sala"

  /salas/{id}:
    patch:
      operationId: updateSala
      tags: [salas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalaUpdate"
      responses:
        "200":
          description: Atualizada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Sala"
    delete:
      operationId: deleteSala
      tags: [salas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removida

  # â”€â”€ DISPONIBILIDADE â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /disponibilidade:
    get:
      operationId: listDisponibilidade
      tags: [disponibilidade]
      parameters:
        - name: professorId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Disponibilidade"
    post:
      operationId: setDisponibilidade
      tags: [disponibilidade]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisponibilidadeInput"
      responses:
        "201":
          description: Criada ou atualizada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Disponibilidade"

  /disponibilidade/lote:
    post:
      operationId: setDisponibilidadeLote
      tags: [disponibilidade]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisponibilidadeLoteInput"
      responses:
        "201":
          description: Itens criados ou atualizados
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Disponibilidade"

  /disponibilidade/{id}:
    delete:
      operationId: deleteDisponibilidade
      tags: [disponibilidade]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removida
# â”€â”€ HORARIO SLOTS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /horario-slots:
    get:
      operationId: listHorarioSlots
      tags: [horario-slots]
      parameters:
        - name: turno
          in: query
          schema:
            type: string
            enum: [matutino, vespertino, noturno]
        - name: nivelEnsino
          in: query
          schema:
            type: string
            enum: [fundamental, medio_tecnico]
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EsquemaHorarioSlot"
    post:
      operationId: setHorarioSlot
      tags: [horario-slots]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EsquemaHorarioSlotInput"
      responses:
        "201":
          description: Criado ou atualizado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EsquemaHorarioSlot"

  /horario-slots/lote:
    post:
      operationId: setHorarioSlotsLote
      tags: [horario-slots]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EsquemaHorarioSlotLoteInput"
      responses:
        "201":
          description: Esquema do turno substituÃ­do
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EsquemaHorarioSlot"

  /horario-slots/{id}:
    delete:
      operationId: deleteHorarioSlot
      tags: [horario-slots]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removido

  # â”€â”€ AULAS FIXAS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /aulas-fixas:
    get:
      operationId: listAulasFixas
      tags: [aulas-fixas]
      parameters:
        - name: turmaId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AulaFixa"
    post:
      operationId: criarAulaFixa
      tags: [aulas-fixas]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AulaFixaInput"
      responses:
        "201":
          description: Criada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AulaFixa"

  /aulas-fixas/{id}:
    delete:
      operationId: deleteAulaFixa
      tags: [aulas-fixas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removida

  # â”€â”€ LIMITES DIÁRIOS PROFESSOR â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /limites-diarios-professor:
    get:
      operationId: listLimitesDiariosProfessor
      tags: [limites-diarios-professor]
      parameters:
        - name: professorId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LimiteDiarioProfessor"
    post:
      operationId: setLimiteDiarioProfessor
      tags: [limites-diarios-professor]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LimiteDiarioProfessorInput"
      responses:
        "201":
          description: Criado ou atualizado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LimiteDiarioProfessor"

  /limites-diarios-professor/{id}:
    delete:
      operationId: deleteLimiteDiarioProfessor
      tags: [limites-diarios-professor]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removido
  # â”€â”€ ESCOLAS / ONBOARDING â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /escolas/planos:
    get:
      operationId: listPlanos
      tags: [escolas]
      responses:
        "200":
          description: Lista de planos disponÃ­veis (RF-ESC, expansÃ£o SaaS)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Plano"

  /escolas/me:
    get:
      operationId: getEscolaAtual
      tags: [escolas]
      responses:
        "200":
          description: Escola vinculada ao usuÃ¡rio/organizaÃ§Ã£o logada, se jÃ¡ cadastrada (RF-ESC-01)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EscolaAtual"

  /escolas:
    post:
      operationId: cadastrarEscola
      tags: [escolas]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EscolaInput"
      responses:
        "200":
          description: Escola atualizada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Escola"
        "201":
          description: Escola criada (onboarding concluÃ­do)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Escola"

  /escolas/assinatura-asaas:
    post:
      operationId: criarAssinaturaAsaas
      tags: [escolas]
      description: >
        Cria (ou reaproveita) o Customer no Asaas e abre uma Subscription pro
        plano escolhido. O Asaas notifica a escola automaticamente por
        e-mail/WhatsApp com o boleto/PIX da primeira cobranca.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssinaturaAsaasInput"
      responses:
        "200":
          description: Assinatura criada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssinaturaAsaasResponse"

  # â”€â”€ PAINEL MASTER SAAS (RF-MASTER-01 a RF-MASTER-03) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  # Opera fora do isolamento por escola de propÃ³sito â€” administra todas as
  # escolas da plataforma. Ver middlewares/requireMaster.ts.
  /master/whoami:
    get:
      operationId: masterWhoami
      tags: [master]
      responses:
        "200":
          description: Se o usuÃ¡rio autenticado Ã© administrador da plataforma
          content:
            application/json:
              schema:
                type: object
                required: [isMaster]
                properties:
                  isMaster: { type: boolean }

  /master/escolas:
    get:
      operationId: listEscolasMaster
      tags: [master]
      responses:
        "200":
          description: Todas as escolas da plataforma, com mÃ©tricas bÃ¡sicas
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EscolaComMetricas"
        "403":
          description: UsuÃ¡rio nÃ£o Ã© administrador da plataforma

  /master/escolas/{id}:
    patch:
      operationId: updateEscolaMaster
      tags: [master]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EscolaMasterUpdateInput"
      responses:
        "200":
          description: Escola atualizada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Escola"

  /master/escolas/{id}/cancelar-assinatura-asaas:
    post:
      operationId: cancelarAssinaturaAsaasMaster
      tags: [master]
      description: >
        Cancela a assinatura da escola no Asaas (chamada de API real) e
        reflete o cancelamento no banco. Encerra a cobrança recorrente de
        verdade -- diferente de só marcar planoAtivo=false.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Assinatura cancelada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Escola"
        "400":
          description: Escola não tem assinatura Asaas ativa
        "404":
          description: Escola não encontrada
        "502":
          description: Falha ao cancelar no Asaas

  /master/planos:
    get:
      operationId: listPlanosMaster
      tags: [master]
      responses:
        "200":
          description: Todos os planos, incluindo inativos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Plano"
    post:
      operationId: createPlano
      tags: [master]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlanoInput"
      responses:
        "201":
          description: Plano criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Plano"

  /master/planos/{id}:
    patch:
      operationId: updatePlano
      tags: [master]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlanoInput"
      responses:
        "200":
          description: Plano atualizado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Plano"

  /master/metrics:
    get:
      operationId: getMasterMetrics
      tags: [master]
      responses:
        "200":
          description: MÃ©tricas agregadas de uso da plataforma (RF-MASTER-03)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MasterMetrics"

  # â”€â”€ CURSOS E MATRIZ CURRICULAR â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /cursos:
    get:
      operationId: listCursos
      tags: [cursos]
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Curso"
    post:
      operationId: createCurso
      tags: [cursos]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CursoInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Curso"

  /cursos/{id}:
    patch:
      operationId: updateCurso
      tags: [cursos]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CursoInput"
      responses:
        "200":
          description: Atualizado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Curso"
    delete:
      operationId: deleteCurso
      tags: [cursos]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removido

  /cursos/{cursoId}/matrizes:
    get:
      operationId: listMatrizesCurriculares
      tags: [cursos]
      parameters:
        - name: cursoId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Lista de matrizes do curso, com itens
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MatrizCurricular"
    post:
      operationId: createMatrizCurricular
      tags: [cursos]
      parameters:
        - name: cursoId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MatrizCurricularInput"
      responses:
        "201":
          description: Criada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatrizCurricular"

  /cursos/{cursoId}/matrizes/{matrizId}:
    delete:
      operationId: deleteMatrizCurricular
      tags: [cursos]
      parameters:
        - name: cursoId
          in: path
          required: true
          schema:
            type: integer
        - name: matrizId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removida
    patch:
      operationId: updateMatrizCurricular
      tags: [cursos]
      parameters:
        - name: cursoId
          in: path
          required: true
          schema:
            type: integer
        - name: matrizId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AtualizarCodigoMatrizInput"
      responses:
        "200":
          description: Atualizada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatrizCurricular"
  /cursos/{cursoId}/matrizes/{matrizId}/itens:
    post:
      operationId: adicionarItemMatriz
      tags: [cursos]
      parameters:
        - name: cursoId
          in: path
          required: true
          schema:
            type: integer
        - name: matrizId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ItemMatrizInput"
      responses:
        "201":
          description: Item adicionado, matriz recalculada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemMatriz"

  /cursos/{cursoId}/matrizes/{matrizId}/itens/{itemId}:
    delete:
      operationId: removerItemMatriz
      tags: [cursos]
      parameters:
        - name: cursoId
          in: path
          required: true
          schema:
            type: integer
        - name: matrizId
          in: path
          required: true
          schema:
            type: integer
        - name: itemId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removido

  # â”€â”€ LICENÃ‡AS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /licencas:
    get:
      operationId: listLicencas
      tags: [licencas]
      parameters:
        - name: professorId
          in: query
          schema:
            type: integer
        - name: ativas
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Licenca"
    post:
      operationId: createLicenca
      tags: [licencas]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LicencaInput"
      responses:
        "201":
          description: Criada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Licenca"

  /licencas/{id}:
    patch:
      operationId: updateLicenca
      tags: [licencas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LicencaUpdate"
      responses:
        "200":
          description: Atualizada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Licenca"
    delete:
      operationId: deleteLicenca
      tags: [licencas]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removida

  # â”€â”€ COMUNICADOS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /calendario-escolar:
    get:
      operationId: listCalendarioEscolar
      tags: [calendario-escolar]
      parameters:
        - name: ano
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Lista de eventos do calendï¿½rio
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CalendarioEscolarEvento"

  /calendario-escolar/trimestres:
    get:
      operationId: listTrimestresLetivos
      tags: [calendario-escolar]
      parameters:
        - name: ano
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Lista de trimestres letivos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TrimestreLetivo"

  /calendario-escolar/carga-horaria:
    get:
      operationId: listCargaHoraria
      tags: [calendario-escolar]
      parameters:
        - name: ano
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Carga horaria cumprida (grade x semanas letivas) vs exigida, por turma e disciplina
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CargaHorariaTurmaDisciplina"
  /comunicados:
    get:
      operationId: listComunicados
      tags: [comunicados]
      parameters:
        - name: turmaId
          in: query
          schema:
            type: integer
        - name: naoLidos
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Comunicado"
    post:
      operationId: createComunicado
      tags: [comunicados]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ComunicadoInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Comunicado"

  /comunicados/{id}/lida:
    patch:
      operationId: marcarComunicadoLido
      tags: [comunicados]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Marcado como lido
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Comunicado"

  /comunicados/{id}:
    delete:
      operationId: deleteComunicado
      tags: [comunicados]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removido

  # â”€â”€ USUÁRIOS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /usuarios:
    get:
      operationId: listUsuarios
      tags: [usuarios]
      responses:
        "200":
          description: Lista
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Usuario"
    post:
      operationId: createUsuario
      tags: [usuarios]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"

  /usuarios/me:
    get:
      operationId: getMe
      tags: [usuarios]
      summary: Retorna o perfil do usuÃ¡rio autenticado
      responses:
        "200":
          description: Meu perfil
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"
        "404":
          description: NÃ£o cadastrado ainda

  /usuarios/{id}:
    patch:
      operationId: updateUsuario
      tags: [usuarios]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioUpdate"
      responses:
        "200":
          description: Atualizado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"

  # â”€â”€ AUDIT LOG â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /audit:
    get:
      operationId: listAuditLogs
      tags: [audit]
      parameters:
        - name: entidade
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: HistÃ³rico
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditLog"

  # â”€â”€ CONFIGURAÃ‡Ã•ES â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /configuracoes:
    get:
      operationId: listConfiguracoes
      tags: [configuracoes]
      responses:
        "200":
          description: ConfiguraÃ§Ãµes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Configuracao"

  /configuracoes/{chave}:
    get:
      operationId: getConfiguracao
      tags: [configuracoes]
      parameters:
        - name: chave
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: ConfiguraÃ§Ã£o
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Configuracao"
    put:
      operationId: upsertConfiguracao
      tags: [configuracoes]
      parameters:
        - name: chave
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfiguracaoInput"
      responses:
        "200":
          description: Salva
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Configuracao"

  # â”€â”€ EXPORTAÃ‡ÃƒO â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /export/grade:
    get:
      operationId: exportGrade
      tags: [export]
      summary: Exporta grade horÃ¡ria em CSV
      parameters:
        - name: turmaId
          in: query
          schema:
            type: integer
        - name: formato
          in: query
          schema:
            type: string
            enum: [csv, json]
      responses:
        "200":
          description: Arquivo CSV ou JSON
          content:
            text/csv:
              schema:
                type: string
            application/json:
              schema:
                type: object

  /export/ponto:
    get:
      operationId: exportPonto
      tags: [export]
      summary: Exporta controle de ponto dos professores em CSV
      parameters:
        - name: professorId
          in: query
          schema:
            type: integer
        - name: mes
          in: query
          schema:
            type: integer
        - name: ano
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: CSV de ponto
          content:
            text/csv:
              schema:
                type: string

  /export/relatorio-seed:
    get:
      operationId: exportRelatorioSeed
      tags: [export]
      summary: Exporta relatÃ³rio no formato SEED do estado
      parameters:
        - name: estado
          in: query
          schema:
            type: string
      responses:
        "200":
          description: RelatÃ³rio
          content:
            application/json:
              schema:
                type: object

  # â”€â”€ CONFLITOS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /conflitos:
    get:
      operationId: listConflitos
      tags: [conflitos]
      summary: Lista todos os conflitos detectados
      responses:
        "200":
          description: Conflitos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Conflito"

  /conflitos/sugestoes:
    get:
      operationId: getConflitosComSugestoes
      tags: [conflitos]
      summary: Conflitos com sugestÃµes algorÃ­tmicas de resoluÃ§Ã£o
      responses:
        "200":
          description: Conflitos com sugestÃµes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConflitoComSugestao"

  # â”€â”€ STATS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  /stats:
    get:
      operationId: getStats
      tags: [stats]
      responses:
        "200":
          description: EstatÃ­sticas
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Stats"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required: [status]

    Professor:
      type: object
      required: [id, nome, email, ativo, createdAt]
      properties:
        id: { type: integer }
        nome: { type: string }
        email: { type: string }
        telefone: { type: ["string", "null"] }
        ativo: { type: boolean }
        disciplinaIds:
          type: array
          items: { type: integer }
        createdAt: { type: string }

    ProfessorInput:
      type: object
      required: [nome, email]
      properties:
        nome: { type: string, minLength: 2 }
        email: { type: string }
        telefone: { type: string }
        disciplinaIds:
          type: array
          items: { type: integer }

    ProfessorUpdate:
      type: object
      properties:
        nome: { type: string }
        email: { type: string }
        telefone: { type: string }
        ativo: { type: boolean }
        disciplinaIds:
          type: array
          items: { type: integer }

    ProfessorCarga:
      type: object
      required: [professorId, totalAulas, porDia]
      properties:
        professorId: { type: integer }
        totalAulas: { type: integer }
        porDia:
          type: object
          additionalProperties: { type: integer }

    Disciplina:
      type: object
      required: [id, nome, cargaSemanal, cor, createdAt]
      properties:
        id: { type: integer }
        nome: { type: string }
        cargaSemanal: { type: integer }
        cor: { type: string }
        codigoSae: { type: ["string", "null"], description: "CÃ³digo SAE oficial (SEED-PR), quando conhecido." }
        sigla: { type: ["string", "null"], description: "Sigla curta usada nas grades PDF compactas (ex: MAT, L.POR)." }
        tipoSalaExigido: { type: ["string", "null"], description: "Quando preenchido, toda aula desta disciplina deve usar uma sala com este mesmo Sala.tipo (ex. laboratorio, quadra)." }
        categoriaCurricularPadrao: { type: ["string", "null"], enum: [BNC, PD, FGB, PFO, IFA, IF, IFP, APF, null], description: "Categoria curricular fixa desta disciplina (nula = flexivel, elegivel para qualquer categoria)." }
        createdAt: { type: string }

    DisciplinaInput:
      type: object
      required: [nome, cargaSemanal]
      properties:
        nome: { type: string, minLength: 2 }
        cargaSemanal: { type: integer, minimum: 1, maximum: 10 }
        cor: { type: string }
        sigla: { type: string }
        codigoSae: { type: string }
        tipoSalaExigido: { type: string }
        categoriaCurricularPadrao: { type: string, enum: [BNC, PD, FGB, PFO, IFA, IF, IFP, APF] }

    DisciplinaUpdate:
      type: object
      properties:
        nome: { type: string }
        cargaSemanal: { type: integer }
        cor: { type: string }
        sigla: { type: ["string", "null"] }
        codigoSae: { type: ["string", "null"] }
        tipoSalaExigido: { type: ["string", "null"] }
        categoriaCurricularPadrao: { type: ["string", "null"], enum: [BNC, PD, FGB, PFO, IFA, IF, IFP, APF, null] }

    Turma:
      type: object
      required: [id, nome, serie, turno, anoLetivo, createdAt]
      properties:
        id: { type: integer }
        nome: { type: string }
        serie: { type: string }
        turno: { type: string, enum: [matutino, vespertino, noturno, integral] }
        anoLetivo: { type: integer }
        modalidade: { type: ["string", "null"] }
        matrizCurricularId: { type: ["integer", "null"], description: "Matriz Curricular aplicada a esta turma (RF-TUR-02), se houver." }
        disciplinaIds:
          type: array
          items: { type: integer }
        disciplinasComCarga:
          type: array
          description: "Carga horÃ¡ria semanal efetiva por disciplina (override da matriz aplicada, com fallback para a carga global)."
          items:
            type: object
            required: [disciplinaId, nome, cargaHorariaSemanal, origemMatriz]
            properties:
              disciplinaId: { type: integer }
              nome: { type: string }
              cargaHorariaSemanal: { type: integer }
              origemMatriz: { type: boolean }
              maxAulasConsecutivasDia: { type: ["integer", "null"], description: "Limite de aulas geminadas desta disciplina, nesta turma, por dia. Nulo = usa o padrÃ£o geral (configuracoes/seed_pr.max_aulas_geminadas_padrao)." }
              grupoCompartilhadoId: { type: ["string", "null"], description: "Quando preenchido, agrupa esta disciplina com a mesma linha de outras turmas para horÃ¡rio simultÃ¢neo (ex. ItinerÃ¡rio Formativo cursado junto)." }
        createdAt: { type: string }

    AplicarMatrizInput:
      type: object
      required: [matrizCurricularId]
      properties:
        matrizCurricularId: { type: integer }

    DisciplinaConfigTurma:
      type: object
      properties:
        maxAulasConsecutivasDia: { type: integer, minimum: 1 }
        grupoCompartilhadoId: { type: string }

    TurmaInput:
      type: object
      required: [nome, serie, turno, anoLetivo]
      properties:
        nome: { type: string, minLength: 1 }
        serie: { type: string }
        turno: { type: string, enum: [matutino, vespertino, noturno, integral] }
        anoLetivo: { type: integer }
        modalidade: { type: string }
        disciplinaIds:
          type: array
          items: { type: integer }
        disciplinasConfig:
          type: object
          description: "ConfiguraÃ§Ã£o extra por disciplina â€” chave Ã© o disciplinaId (como string). SÃ³ tem efeito para disciplinas presentes em disciplinaIds."
          additionalProperties:
            $ref: "#/components/schemas/DisciplinaConfigTurma"

    TurmaUpdate:
      type: object
      properties:
        nome: { type: string }
        serie: { type: string }
        turno: { type: string, enum: [matutino, vespertino, noturno, integral] }
        anoLetivo: { type: integer }
        modalidade: { type: string }
        disciplinaIds:
          type: array
          items: { type: integer }
        disciplinasConfig:
          type: object
          description: "ConfiguraÃ§Ã£o extra por disciplina â€” chave Ã© o disciplinaId (como string). Pode ser enviado mesmo sem disciplinaIds, para sÃ³ atualizar a configuraÃ§Ã£o de vÃ­nculos jÃ¡ existentes."
          additionalProperties:
            $ref: "#/components/schemas/DisciplinaConfigTurma"

    HorarioSlot:
      type: object
      required: [id, turmaId, disciplinaId, professorId, diaSemana, numeroAula]
      properties:
        id: { type: integer }
        turmaId: { type: integer }
        turma:
          $ref: "#/components/schemas/Turma"
        disciplinaId: { type: integer }
        disciplina:
          $ref: "#/components/schemas/Disciplina"
        professorId: { type: integer }
        professor:
          $ref: "#/components/schemas/Professor"
        diaSemana: { type: integer, minimum: 0, maximum: 4 }
        numeroAula: { type: integer, minimum: 1, maximum: 8 }
        sala: { type: ["string", "null"] }

    HorarioSlotInput:
      type: object
      required: [turmaId, disciplinaId, professorId, diaSemana, numeroAula]
      properties:
        turmaId: { type: integer }
        disciplinaId: { type: integer }
        professorId: { type: integer }
        diaSemana: { type: integer, minimum: 0, maximum: 4 }
        numeroAula: { type: integer, minimum: 1, maximum: 8 }
        sala: { type: string }

    HorarioExperimental:
      type: object
      required: [id, nome, turmaId, disciplinaId, professorId, diaSemana, numeroAula]
      properties:
        id: { type: integer }
        nome: { type: string }
        turmaId: { type: integer }
        disciplinaId: { type: integer }
        professorId: { type: integer }
        diaSemana: { type: integer }
        numeroAula: { type: integer }
        sala: { type: ["string", "null"] }
        ativo: { type: boolean }

    HorarioExperimentalInput:
      type: object
      required: [nome, turmaId, disciplinaId, professorId, diaSemana, numeroAula]
      properties:
        nome: { type: string }
        turmaId: { type: integer }
        disciplinaId: { type: integer }
        professorId: { type: integer }
        diaSemana: { type: integer }
        numeroAula: { type: integer }
        sala: { type: string }

    GerarHorarioInput:
      type: object
      required: [turmaId]
      properties:
        turmaId: { type: integer }
        aulaspordia: { type: integer, minimum: 4, maximum: 8 }
        substituir: { type: boolean }
        reduzirJanelas: { type: boolean }
        fatorPedagogico: { type: boolean }
        compactarCargaHoraria: { type: boolean }
        experimental: { type: boolean }
        nomeExperimental: { type: string }

    GerarHorarioResult:
      type: object
      required: [slotsGerados, conflitos]
      properties:
        slotsGerados: { type: integer }
        conflitos:
          type: array
          items: { type: string }
        horario:
          type: array
          items:
            $ref: "#/components/schemas/HorarioSlot"

    Sala:
      type: object
      required: [id, nome, tipo, capacidade, ativa]
      properties:
        id: { type: integer }
        nome: { type: string }
        tipo: { type: string, enum: [sala_aula, laboratorio, quadra, informatica, auditorio, biblioteca, sala_arte, outro] }
        capacidade: { type: integer }
        ativa: { type: boolean }
        observacoes: { type: ["string", "null"] }

    SalaInput:
      type: object
      required: [nome]
      properties:
        nome: { type: string, minLength: 1 }
        tipo: { type: string, enum: [sala_aula, laboratorio, quadra, informatica, auditorio, biblioteca, sala_arte, outro] }
        capacidade: { type: integer }
        observacoes: { type: string }

    SalaUpdate:
      type: object
      properties:
        nome: { type: string }
        tipo: { type: string }
        capacidade: { type: integer }
        ativa: { type: boolean }
        observacoes: { type: string }

    Disponibilidade:
      type: object
      required: [id, professorId, diaSemana, horarioSlot, disponivel]
      properties:
        id: { type: integer }
        professorId: { type: integer }
        diaSemana: { type: integer, minimum: 0, maximum: 6 }
        horarioSlot: { type: integer, minimum: 1 }
        disponivel: { type: boolean }
        motivo: { type: ["string", "null"] }
        turno: { type: ["string", "null"], enum: [matutino, vespertino, noturno, null], description: "Turno deste bloqueio â€” necessÃ¡rio para validar concentraÃ§Ã£o de hora-atividade no mesmo turno das aulas (ResoluÃ§Ã£o SEED n.Âº 7.200/2025, art. 11, Â§4Âº)." }
        horaAtividadeObrigatoria: { type: boolean, default: false, description: "true quando este bloqueio representa Hora-Atividade obrigatÃ³ria, nÃ£o outro tipo de indisponibilidade." }

    DisponibilidadeInput:
      type: object
      required: [professorId, diaSemana, horarioSlot]
      properties:
        professorId: { type: integer }
        diaSemana: { type: integer, minimum: 0, maximum: 6 }
        horarioSlot: { type: integer, minimum: 1 }
        disponivel: { type: boolean, default: true }
        motivo: { type: string }
        turno: { type: string, enum: [matutino, vespertino, noturno] }
        horaAtividadeObrigatoria: { type: boolean, default: false }

    DisponibilidadeLoteInput:
      type: object
      required: [professorId, itens]
      properties:
        professorId: { type: integer }
        itens:
          type: array
          items:
            type: object
            required: [diaSemana, horarioSlot]
            properties:
              diaSemana: { type: integer, minimum: 0, maximum: 6 }
              horarioSlot: { type: integer, minimum: 1 }
              disponivel: { type: boolean, default: true }
              motivo: { type: string }
              turno: { type: string, enum: [matutino, vespertino, noturno] }
              horaAtividadeObrigatoria: { type: boolean, default: false }
    DisciplinaCatalogo:
      type: object
      required: [id, nome, cargaSemanalSugerida]
      properties:
        id: { type: integer }
        nome: { type: string }
        codigoSae: { type: string, nullable: true }
        categoriaCurricularPadrao: { type: string, enum: [BNC, PD, FGB, PFO, IFA, IF, IFP, APF], nullable: true }
        cargaSemanalSugerida: { type: integer, default: 2 }
        tipoSalaExigido: { type: string, nullable: true }
    AdicionarDisciplinasCatalogoInput:
      type: object
      required: [catalogoIds]
      properties:
        catalogoIds:
          type: array
          items: { type: integer }
    AdicionarDisciplinasCatalogoResponse:
      type: object
      required: [criadas, jaExistiam]
      properties:
        criadas:
          type: array
          items:
            $ref: "#/components/schemas/Disciplina"
        jaExistiam: { type: integer }
    EsquemaHorarioSlot:
      type: object
      required: [id, turno, numeroAula, horaInicio, duracaoMinutos]
      properties:
        id: { type: integer }
        turno: { type: string, enum: [matutino, vespertino, noturno] }
        nivelEnsino: { type: string, enum: [fundamental, medio_tecnico], nullable: true }
        numeroAula: { type: integer, minimum: 1 }
        horaInicio: { type: string, description: "Formato HH:MM ou HH:MM:SS" }
        duracaoMinutos: { type: integer, default: 50 }

    EsquemaHorarioSlotInput:
      type: object
      required: [turno, numeroAula, horaInicio]
      properties:
        turno: { type: string, enum: [matutino, vespertino, noturno] }
        nivelEnsino: { type: string, enum: [fundamental, medio_tecnico], nullable: true }
        numeroAula: { type: integer, minimum: 1 }
        horaInicio: { type: string, description: "Formato HH:MM ou HH:MM:SS" }
        duracaoMinutos: { type: integer, default: 50 }

    EsquemaHorarioSlotLoteInput:
      type: object
      required: [turno, slots]
      properties:
        turno: { type: string, enum: [matutino, vespertino, noturno] }
        nivelEnsino: { type: string, enum: [fundamental, medio_tecnico], nullable: true }
        slots:
          type: array
          items:
            type: object
            required: [numeroAula, horaInicio]
            properties:
              numeroAula: { type: integer, minimum: 1 }
              horaInicio: { type: string, description: "Formato HH:MM ou HH:MM:SS" }
              duracaoMinutos: { type: integer, default: 50 }
    AulaFixa:
      type: object
      required: [id, turmaId, disciplinaId, professorId, diaSemana, numeroAula, anoLetivo]
      properties:
        id: { type: integer }
        turmaId: { type: integer }
        disciplinaId: { type: integer }
        professorId: { type: integer }
        diaSemana: { type: integer, minimum: 0, maximum: 6 }
        numeroAula: { type: integer, minimum: 1 }
        anoLetivo: { type: integer }

    AulaFixaInput:
      type: object
      required: [turmaId, disciplinaId, professorId, diaSemana, numeroAula, anoLetivo]
      properties:
        turmaId: { type: integer }
        disciplinaId: { type: integer }
        professorId: { type: integer }
        diaSemana: { type: integer, minimum: 0, maximum: 6 }
        numeroAula: { type: integer, minimum: 1 }
        anoLetivo: { type: integer }

    LimiteDiarioProfessor:
      type: object
      required: [id, professorId, maxAulasPorDia]
      properties:
        id: { type: integer }
        professorId: { type: integer }
        turmaId: { type: ["integer", "null"], description: "Nulo = padrÃ£o do professor, vÃ¡lido em qualquer turma. Preenchido = override especÃ­fico por turma." }
        maxAulasPorDia: { type: integer, minimum: 1 }

    LimiteDiarioProfessorInput:
      type: object
      required: [professorId, maxAulasPorDia]
      properties:
        professorId: { type: integer }
        turmaId: { type: ["integer", "null"], default: null, description: "Nulo = padrÃ£o do professor, vÃ¡lido em qualquer turma. Preenchido = override especÃ­fico por turma." }
        maxAulasPorDia: { type: integer, minimum: 1 }
    Curso:
      type: object
      required: [id, nome, nivel]
      properties:
        id: { type: integer }
        nome: { type: string }
        codigoCurso: { type: ["string", "null"] }
        nivel: { type: string, enum: [fundamental, medio, tecnico, normal_magisterio] }

    CursoInput:
      type: object
      required: [nome]
      properties:
        nome: { type: string, minLength: 1 }
        codigoCurso: { type: string }
        nivel: { type: string, enum: [fundamental, medio, tecnico, normal_magisterio], default: fundamental }

    ItemMatriz:
      type: object
      required: [id, disciplinaId, categoriaCurricular, cargaHorariaSemanal, ehPadraoDoGrupo, obrigatoria]
      properties:
        id: { type: integer }
        disciplinaId: { type: integer }
        disciplina:
          $ref: "#/components/schemas/Disciplina"
        categoriaCurricular:
          type: string
          enum: [BNC, PD, FGB, PFO, IFA, IF, IFP, APF]
        cargaHorariaSemanal: { type: integer }
        grupoDisciplina: { type: ["string", "null"] }
        ehPadraoDoGrupo: { type: boolean }
        obrigatoria: { type: boolean }

    ItemMatrizInput:
      type: object
      required: [disciplinaId, cargaHorariaSemanal]
      properties:
        disciplinaId: { type: integer }
        categoriaCurricular:
          type: string
          enum: [BNC, PD, FGB, PFO, IFA, IF, IFP, APF]
          default: BNC
        cargaHorariaSemanal: { type: integer, minimum: 1 }
        grupoDisciplina: { type: string }
        ehPadraoDoGrupo: { type: boolean, default: false }
        obrigatoria: { type: boolean, default: true }

    MatrizCurricular:
      type: object
      required: [id, cursoId, serieAno, cargaHorariaSemanalTotal, itens]
      properties:
        id: { type: integer }
        cursoId: { type: integer }
        serieAno: { type: string }
        cargaHorariaSemanalTotal: { type: integer }
        codigoMatrizSere: { type: ["string", "null"], description: "Codigo unico de matriz atribuido pelo Estado (SERE/SAE), quando ja registrado." }
        itens:
          type: array
          items:
            $ref: "#/components/schemas/ItemMatriz"

    MatrizCurricularInput:
      type: object
      required: [serieAno]
      properties:
        serieAno: { type: string, minLength: 1 }
        codigoMatrizSere: { type: string, description: "Codigo unico de matriz atribuido pelo Estado (SERE/SAE), quando ja registrado." }
        itens:
          type: array
          items:
            $ref: "#/components/schemas/ItemMatrizInput"
          default: []

    AtualizarCodigoMatrizInput:
      type: object
      required: [codigoMatrizSere]
      properties:
        codigoMatrizSere:
          type: ["string", "null"]
    Plano:
      type: object
      required: [id, nome, precoMensal, maxProfessores, maxTurmas, temIA, temExport, temImport, ativo]
      properties:
        id: { type: integer }
        nome: { type: string }
        precoMensal: { type: integer, description: "Preço mensal em centavos. 0 = plano gratuito (Piloto)." }
        precoAnual: { type: ["integer", "null"], description: "Preço anual em centavos, com desconto. Nulo enquanto não configurado." }
        maxProfessores: { type: integer }
        maxTurmas: { type: integer }
        temIA: { type: boolean }
        temExport: { type: boolean }
        temImport: { type: boolean }
        ativo: { type: boolean }
        stripePriceIdMensal: { type: ["string", "null"], description: "Price ID do Stripe pro plano mensal." }
        stripePriceIdAnual: { type: ["string", "null"], description: "Price ID do Stripe pro plano anual." }

    Escola:
      type: object
      required: [id, nomeFantasia, estado, modalidade, planoAtivo]
      properties:
        id: { type: string }
        nomeFantasia: { type: string }
        cnpj: { type: ["string", "null"] }
        cidade: { type: ["string", "null"] }
        estado: { type: string }
        modalidade: { type: string, enum: [regular, tecnica, eja, normal_magisterio] }
        codigoInep: { type: ["string", "null"], description: "Código INEP oficial da escola perante o MEC." }
        nre: { type: ["string", "null"], description: "Núcleo Regional de Educação (SEED-PR) ou equivalente estadual." }
        turnosOfertados: { type: ["string", "null"], description: "Lista separada por vírgula, ex: 'matutino,vespertino'." }
        resolucaoSeedPr: { type: ["string", "null"], description: "Número da resolução SEED-PR (ou equivalente estadual) que rege a escola." }
        planoId: { type: ["integer", "null"] }
        clerkOrgId: { type: ["string", "null"] }
        stripeCustomerId: { type: ["string", "null"] }
        stripeSubscriptionId: { type: ["string", "null"] }
        emailContato: { type: ["string", "null"], description: "E-mail de contato geral da escola." }
        telefoneContato: { type: ["string", "null"] }
        emailCobranca: { type: ["string", "null"], description: "E-mail específico usado pelo Asaas pra notificar a escola (boleto/PIX). Cai pra emailContato quando vazio." }
        asaasCustomerId: { type: ["string", "null"] }
        asaasSubscriptionId: { type: ["string", "null"] }
        asaasStatusAssinatura: { type: ["string", "null"], enum: [em_dia, pendente, atrasada, cancelada, null], description: "Alimentado pelo webhook do Asaas a cada evento de cobrança." }
        asaasProximoVencimento: { type: ["string", "null"], format: date-time }
        planoAtivo: { type: boolean }
        trialEndsAt: { type: ["string", "null"], format: date-time }
        isenta: { type: boolean, description: "Escola isenta de bloqueio por trial vencido/sem assinatura ativa (ex: piloto)." }

    EscolaAtual:
      type: object
      required: [cadastrada]
      properties:
        cadastrada: { type: boolean, description: "false quando o usuÃ¡rio/organizaÃ§Ã£o ainda nÃ£o passou pelo onboarding." }
        id: { type: string }
        nomeFantasia: { type: string }
        cidade: { type: ["string", "null"] }
        estado: { type: string }
        modalidade: { type: string }
        codigoInep: { type: ["string", "null"] }
        nre: { type: ["string", "null"] }
        turnosOfertados: { type: ["string", "null"] }
        resolucaoSeedPr: { type: ["string", "null"] }
        planoAtivo: { type: boolean }
        trialEndsAt: { type: ["string", "null"], format: date-time }
        emailContato: { type: ["string", "null"] }
        telefoneContato: { type: ["string", "null"] }
        emailCobranca: { type: ["string", "null"] }
        plano:
          $ref: "#/components/schemas/Plano"

    EscolaInput:
      type: object
      required: [nomeFantasia]
      properties:
        nomeFantasia: { type: string, minLength: 2 }
        cnpj: { type: string }
        cidade: { type: string }
        estado: { type: string, default: "SP" }
        modalidade: { type: string, enum: [regular, tecnica, eja, normal_magisterio], default: regular }
        emailContato: { type: string, format: email }
        telefoneContato: { type: string }
        emailCobranca: { type: string, format: email }
        codigoInep: { type: string }
        nre: { type: string }
        turnosOfertados: { type: string, description: "Lista separada por vírgula, ex: 'matutino,vespertino'." }
        resolucaoSeedPr: { type: string }

    Licenca:
      type: object
      required: [id, professorId, dataInicio, dataFim, tipo, aprovada]
      properties:
        id: { type: integer }
        professorId: { type: integer }
        professor:
          $ref: "#/components/schemas/Professor"
        professorSubstitutoId: { type: ["integer", "null"] }
        professorSubstituto:
          $ref: "#/components/schemas/Professor"
        dataInicio: { type: string }
        dataFim: { type: string }
        tipo: { type: string, enum: [medica, maternidade, paternidade, capacitacao, outros] }
        motivo: { type: ["string", "null"] }
        aprovada: { type: boolean }
        comunicadoEnviado: { type: boolean }

    LicencaInput:
      type: object
      required: [professorId, dataInicio, dataFim, tipo]
      properties:
        professorId: { type: integer }
        professorSubstitutoId: { type: integer }
        dataInicio: { type: string }
        dataFim: { type: string }
        tipo: { type: string, enum: [medica, maternidade, paternidade, capacitacao, outros] }
        motivo: { type: string }
        aprovada: { type: boolean }

    LicencaUpdate:
      type: object
      properties:
        professorSubstitutoId: { type: integer }
        dataInicio: { type: string }
        dataFim: { type: string }
        tipo: { type: string }
        motivo: { type: string }
        aprovada: { type: boolean }

    Comunicado:
      type: object
      required: [id, titulo, mensagem, tipo, lida, autorNome, createdAt]
      properties:
        id: { type: integer }
        titulo: { type: string }
        mensagem: { type: string }
        tipo: { type: string, enum: [geral, licenca, conflito, horario, urgente] }
        turmaId: { type: ["integer", "null"] }
        professorId: { type: ["integer", "null"] }
        lida: { type: boolean }
        autorNome: { type: string }
        createdAt: { type: string }

    ComunicadoInput:
      type: object
      required: [titulo, mensagem]
      properties:
        titulo: { type: string, minLength: 2 }
        mensagem: { type: string, minLength: 5 }
        tipo: { type: string, enum: [geral, licenca, conflito, horario, urgente] }
        turmaId: { type: integer }
        professorId: { type: integer }
        autorNome: { type: string }

    CalendarioEscolarEvento:
      type: object
      required: [id, ano, data, tipo, descricao, diaLetivo]
      properties:
        id: { type: integer }
        ano: { type: integer }
        data: { type: string }
        tipo: { type: string }
        descricao: { type: string }
        diaLetivo: { type: boolean }

    TrimestreLetivo:
      type: object
      required: [id, ano, trimestre, dataInicio, dataFim, diasLetivos]
      properties:
        id: { type: integer }
        ano: { type: integer }
        trimestre: { type: integer }
        dataInicio: { type: string }
        dataFim: { type: string }
        diasLetivos: { type: integer }

    CargaHorariaTurmaDisciplina:
      type: object
      required: [turmaId, turmaNome, disciplinaId, disciplinaNome, cargaSemanalExigida, aulasSemanaGrid, trimestres, totalCumprido, totalExigido, status]
      properties:
        turmaId: { type: integer }
        turmaNome: { type: string }
        disciplinaId: { type: integer }
        disciplinaNome: { type: string }
        cargaSemanalExigida: { type: integer }
        aulasSemanaGrid: { type: integer }
        trimestres:
          type: array
          items:
            type: object
            required: [trimestre, semanasLetivas, cumprido, exigido, status]
            properties:
              trimestre: { type: integer }
              semanasLetivas: { type: number }
              cumprido: { type: integer }
              exigido: { type: integer }
              status: { type: string, enum: [ok, insuficiente] }
        totalCumprido: { type: integer }
        totalExigido: { type: integer }
        status: { type: string, enum: [ok, insuficiente] }
    Usuario:
      type: object
      required: [id, clerkId, nome, email, perfil, ativo]
      properties:
        id: { type: integer }
        clerkId: { type: string }
        nome: { type: string }
        email: { type: string }
        perfil: { type: string, enum: [direcao, coordenacao, professor, secretaria] }
        ativo: { type: boolean }
        createdAt: { type: string }

    UsuarioInput:
      type: object
      required: [clerkId, nome, email]
      properties:
        clerkId: { type: string }
        nome: { type: string }
        email: { type: string }
        perfil: { type: string, enum: [direcao, coordenacao, professor, secretaria] }

    UsuarioUpdate:
      type: object
      properties:
        nome: { type: string }
        perfil: { type: string, enum: [direcao, coordenacao, professor, secretaria] }
        ativo: { type: boolean }

    AuditLog:
      type: object
      required: [id, entidade, acao, createdAt]
      properties:
        id: { type: integer }
        entidade: { type: string }
        entidadeId: { type: ["integer", "null"] }
        acao: { type: string }
        dadosAnteriores: {}
        dadosNovos: {}
        usuarioId: { type: ["string", "null"] }
        usuarioNome: { type: ["string", "null"] }
        ip: { type: ["string", "null"] }
        createdAt: { type: string }

    Configuracao:
      type: object
      required: [id, chave]
      properties:
        id: { type: integer }
        chave: { type: string }
        valor: {}
        descricao: { type: ["string", "null"] }

    ConfiguracaoInput:
      type: object
      required: [valor]
      properties:
        valor: {}
        descricao: { type: string }

    Conflito:
      type: object
      required: [tipo, descricao, gravidade]
      properties:
        tipo: { type: string }
        descricao: { type: string }
        gravidade: { type: string, enum: [critico, alto, medio, baixo] }
        turmaId: { type: ["integer", "null"] }
        professorId: { type: ["integer", "null"] }
        diaSemana: { type: ["integer", "null"] }
        numeroAula: { type: ["integer", "null"] }

    ConflitoComSugestao:
      type: object
      required: [conflito, sugestoes]
      properties:
        conflito:
          $ref: "#/components/schemas/Conflito"
        sugestoes:
          type: array
          items: { type: string }

    Stats:
      type: object
      required: [totalProfessores, totalTurmas, totalDisciplinas, turmasSemHorario, totalConflitos, aulasDistribuidas]
      properties:
        totalProfessores: { type: integer }
        totalTurmas: { type: integer }
        totalDisciplinas: { type: integer }
        turmasSemHorario: { type: integer }
        totalConflitos: { type: integer }
        aulasDistribuidas: { type: integer }
        totalSalas: { type: integer }
        licencasAtivas: { type: integer }
        comunicadosNaoLidos: { type: integer }

    EscolaComMetricas:
      allOf:
        - $ref: "#/components/schemas/Escola"
        - type: object
          required: [totalProfessores, totalTurmas]
          properties:
            plano:
              anyOf:
                - $ref: "#/components/schemas/Plano"
                - type: "null"
            totalProfessores: { type: integer }
            totalTurmas: { type: integer }

    EscolaMasterUpdateInput:
      type: object
      properties:
        planoId: { type: ["integer", "null"] }
        planoAtivo: { type: boolean }
        isenta: { type: boolean }
        emailContato: { type: ["string", "null"], format: email }
        telefoneContato: { type: ["string", "null"] }
        emailCobranca: { type: ["string", "null"], format: email }

    AssinaturaAsaasInput:
      type: object
      required: [planoId, periodicidade]
      properties:
        planoId: { type: integer }
        periodicidade: { type: string, enum: [mensal, anual] }

    AssinaturaAsaasResponse:
      type: object
      required: [mensagem, asaasSubscriptionId]
      properties:
        mensagem: { type: string }
        asaasSubscriptionId: { type: string }

    PlanoInput:
      type: object
      required: [nome]
      properties:
        nome: { type: string, minLength: 1 }
        precoMensal: { type: integer, minimum: 0, default: 0 }
        precoAnual: { type: ["integer", "null"], minimum: 0 }
        maxProfessores: { type: integer, minimum: 1, default: 10 }
        maxTurmas: { type: integer, minimum: 1, default: 5 }
        temIA: { type: boolean, default: false }
        temExport: { type: boolean, default: false }
        temImport: { type: boolean, default: false }
        ativo: { type: boolean, default: true }
        stripePriceIdMensal: { type: string }
        stripePriceIdAnual: { type: string }

    MasterMetrics:
      type: object
      required: [totalEscolas, escolasAtivas, escolasInativas, escolasEmTrial, totalProfessores, totalTurmas, totalAulasDistribuidas, totalUsuarios, mensagensEnviadasParaIA]
      properties:
        totalEscolas: { type: integer }
        escolasAtivas: { type: integer }
        escolasInativas: { type: integer }
        escolasEmTrial: { type: integer }
        totalProfessores: { type: integer }
        totalTurmas: { type: integer }
        totalAulasDistribuidas: { type: integer }
        totalUsuarios: { type: integer }
        mensagensEnviadasParaIA: { type: integer }









